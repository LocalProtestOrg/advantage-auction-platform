"""Phase 3P creative families rendered with Pillow (no browser): ENVIRONMENTAL_PHOTO and ACQUISITION.

These renderers compose ONLY assets handed to them (event-bound photographs, Advantage.Bid-owned representative
objects, a production screenshot) with the Owner-approved Advantage.Bid brand frame. They never read the reference
library, never generate imagery, never edit RGB of merchandise. Every text string drawn is returned in the layout so
the seller-mark gate and the text-budget audit can check exactly what appears on the canvas.
"""

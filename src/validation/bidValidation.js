// Bid validation stubs
module.exports = {
  validateBidAmount(currentBidCents, bidAmountCents, incrementLadder) {
    // stub: check against increment ladder and minimum increments
    return { valid: true, errors: [] };
  }
};

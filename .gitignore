# ── Dependencies ──────────────────────────────────────────────────────────────
node_modules

# ── Environment / Secrets ─────────────────────────────────────────────────────
.env
.env.local
.env.production
.env.staging
.env.test
.env.*.local
secrets/
credentials/
*.pem
*.key
*.cert
*.p12
*.pfx

# ── Logs ──────────────────────────────────────────────────────────────────────
*.log
logs/
server.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Scratch / Temp files ──────────────────────────────────────────────────────
*.txt.bak
scratch/
temp/
tmp/
tmp-*/
check-*.js
debug-*.js
get-*.js
_*.js
*For AAC.txt

# ── Build output ──────────────────────────────────────────────────────────────
.next
dist
build
coverage

# ── Playwright ────────────────────────────────────────────────────────────────
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
# governance-regression.spec.js writes this per run; signoff receipts attach
# to release notes or commit messages, not to source control.
governance-summary.json

# ── OS / Editor ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/settings.json
.idea/

# Historical archive — local import source/working data (~142MB: images, CSVs, zips;
# re-hosted on Cloudinary, seeded via scripts; not needed in the repo or on the server).
docs/historical-auctions/archive/

# Generated auction report PDFs (build artifacts, not source).
reports/
